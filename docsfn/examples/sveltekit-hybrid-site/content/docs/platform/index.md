---
title: Platform Overview
description: Infrastructure and operational guidance with a different sidebar.
---

# Platform Overview

This section uses a different sidebar from product docs even though both live under the same `/docs/[...slug]` SvelteKit route.

## Why

The sidebar is resolved from the route itself, so `/docs/platform/...` can have a completely different navigation tree from `/docs/product/...`.
